CREATE DATABASE SampleDatabaseDb;
USE SampleDatabaseDb;

CREATE TABLE StudentTbl (Student_ID INT PRIMARY KEY, Student_Name VARCHAR(50), Department VARCHAR(50), Picpath Text);

SELECT * FROM StudentTbl;